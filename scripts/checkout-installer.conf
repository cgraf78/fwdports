# shellcheck shell=bash
# shellcheck disable=SC2034
# This opt-in is intentionally tiny: the shared provider owns safe checkout
# acquisition, while this repository owns only the one-link publication policy.
CHECKOUT_INSTALLER_ENABLED=true
CHECKOUT_INSTALLER_REPO=cgraf78/fwdports
